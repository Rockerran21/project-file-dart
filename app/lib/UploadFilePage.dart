import 'package:flutter/material.dart';

class UploadFilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload File'),
      ),
      body: Center(
        child: Text('This is the Upload File Page'),
      ),
    );
  }
}
