import 'package:flutter/material.dart';

class Plus2CertificateUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload +2 Certificate'),
      ),
      body: Center(
        child: Text('This is the Upload +2 Certificate Page'),
      ),
    );
  }
}
