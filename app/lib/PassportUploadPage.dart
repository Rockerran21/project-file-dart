import 'package:flutter/material.dart';

class PassportUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload Passport'),
      ),
      body: Center(
        child: Text('This is the Upload Passport Page'),
      ),
    );
  }
}
