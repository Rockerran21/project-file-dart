import 'package:flutter/material.dart';

class CitizenshipUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload Citizenship'),
      ),
      body: Center(
        child: Text('This is the Upload Citizenship Page'),
      ),
    );
  }
}
