import 'package:flutter/material.dart';

class SeeProgressPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('See Progress'),
      ),
      body: Center(
        child: Text('This is the See Progress Page'),
      ),
    );
  }
}
