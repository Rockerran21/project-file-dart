import 'package:flutter/material.dart';

class SeeCertificateUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload SEE Certificate'),
      ),
      body: Center(
        child: Text('This is the Upload SEE Certificate Page'),
      ),
    );
  }
}
